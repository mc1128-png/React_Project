const authMiddleWare = (req, res, next) => {
    // 01 获取jwt
    // 1) header 获取authorization = token jwt
    // 2）获取jwt
    // 3）

    // 02 key 验证token ：解签

    // 03 解签成功
    // return next()

    // 04 解签失败
    // return next(error)
}

module.exports = authMiddleWare